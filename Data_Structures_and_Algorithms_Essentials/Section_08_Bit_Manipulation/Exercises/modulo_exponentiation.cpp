/*
 * Given three numbers x, y, and mod, compute x
 * raised to power y module mod (x^y)%mod.
 */
#include <iostream>

int power(int x, int y, int mod) {
}

int main() {
    std::cout << std::boolalpha;
    std::cout << (power(12, 25, 1007) == 9603) << '\n';
}
